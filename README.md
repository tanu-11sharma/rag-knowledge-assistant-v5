# rag-knowledge-assistant-v5
v5 twist: hybrid TF-IDF + exact-token-match boost (satus codes, header names) with confidence tiers and ross-document citations, over a synthetic API-docs kowledge base. FastAPI + pytest, zero API keys.
